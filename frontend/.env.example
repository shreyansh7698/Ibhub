# Copy to .env and adjust.
VITE_API_BASE_URL=http://localhost:8000

# Shown as a login hint on the admin sign-in screen. Not used for auth itself
# (the real admin account/password lives in the backend).
VITE_DEMO_ADMIN_EMAIL=admin@theibhub.com
VITE_DEMO_ADMIN_PASSWORD=ibhub-demo
